"""gmt-mcp — PyGMT (Generic Mapping Tools) map-making tools as an MCP server.

Mount only this server when you want map/plotting capabilities:

    claude mcp add gmt -- uvx gmt-mcp

Requires a working PyGMT install, which in turn requires the GMT binary on
PATH (``brew install gmt`` on macOS).
"""

__version__ = "0.1.0"
